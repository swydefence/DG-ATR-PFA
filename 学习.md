111111
````
@Slf4j
@Component(value = "procedureService")
public class ProcedureService implements SimpleActionService {
@Autowired
private VariableManager variableManager;

    @Autowired
    private DataBaseManager dataBaseManager;

    @Override
    public ActionServiceDef getActionServiceDef(ServiceContext context) {
        final DefaultActionServiceDef serviceDef = new DefaultActionServiceDef();

        final DefaultSimpleParamDef paramDef1 = new DefaultSimpleParamDef();
        updateProcedureCodeParam(context, paramDef1);
        serviceDef.getParams().add(paramDef1);
        final DefaultParamsGroupDef groupDef2 = new DefaultParamsGroupDef();
        updateRequestParam(context, groupDef2);
        serviceDef.getParams().add(groupDef2);
        final DefaultParamsGroupDef groupDef3 = new DefaultParamsGroupDef();
        updateCacheParam(context, groupDef3);
        serviceDef.getParams().add(groupDef3);
        final DefaultParamsGroupDef groupDef4 = new DefaultParamsGroupDef();
        updateCondParam(context, groupDef4);
        serviceDef.getParams().add(groupDef4);

        serviceDef.setParamChangeHandler(this::paramChangeHandler);
        serviceDef.setBranchResolver((context1, inputParams) -> branchResolver(inputParams));
        return serviceDef;
    }
````